server:
  port: 0

spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:postgresql://localhost:6432/main
    username: main
    password: main

stats-server:
  url: http://stats-server #http://localhost:9090

logging:
  level:
    root: INFO
    ru.practicum.explorewithme.main: DEBUG